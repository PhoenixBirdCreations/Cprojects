#include "prqueue.h"

//Description of function arguments:
void ExitError(char *miss, int errcode); //error message, error code
int read(node** nodes, char *name); //pointer to array of nodes, name of file with the graph